# PQC_HNDL_identifier

 ## Environment Setup 
 ### First, Check What You Have
 ### Open your terminal and run these one by one:
```
bashpython3 --version      # Need 3.8+
pip --version
openssl version        # Should already be installed
```

 ### Install Your Tools
```
bashpip install cryptography
pip install pyOpenSSL
pip install requests
pip install dnspython
pip install rich
```
